name: Build Windows Desktop App

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g., 0.7.0)'
        required: false
        default: '0.7.0'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller
        pip install -r requirements-lite.txt

    - name: Get version
      id: version
      shell: bash
      run: |
        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          echo "VERSION=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
        else
          echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
        fi

    - name: Build desktop application
      run: |
        python build.py
      env:
        PYTHONPATH: .
        BUILD_VERSION: ${{ steps.version.outputs.VERSION }}

    - name: Create release package
      shell: bash
      run: |
        cd release
        7z a -tzip ../mirolaw-${{ steps.version.outputs.VERSION }}-x64-Windows.zip *

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: mirolaw-desktop
        path: release/

    - name: Create Release
      if: github.event_name == 'push'
      uses: softprops/action-gh-release@v1
      with:
        files: |
          release/mirolaw-${{ steps.version.outputs.VERSION }}-x64-Windows.exe
          mirolaw-${{ steps.version.outputs.VERSION }}-x64-Windows.zip
        body: |
          ## MiroLaw v${{ steps.version.outputs.VERSION }} - Desktop Application

          ### Download
          | File | Description |
          |------|-------------|
          | `mirolaw-${{ steps.version.outputs.VERSION }}-x64-Windows.exe` | Desktop app, double-click to run |
          | `mirolaw-${{ steps.version.outputs.VERSION }}-x64-Windows.zip` | Full package |

          ### Usage
          1. Download the exe file
          2. Double-click to launch
          3. A desktop window opens automatically
          4. System tray icon for background monitoring

          ### Requirements
          - Windows 10/11 (64-bit)
          - WebView2 runtime (pre-installed on Windows 10/11)

          ### Features
          - Risk Prediction, Real-time Alerts, Legal Search
          - Fine Prediction, AI Suggestions
          - System Tray, Notification Support

          ### Docs
          - [README](https://github.com/23xxCh/Mirolaw#readme)
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
